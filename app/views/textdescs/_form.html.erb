<%= form_for(@textdesc) do |f| %>
  <% if @textdesc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@textdesc.errors.count, "error") %> prohibited this textdesc from being saved:</h2>

      <ul>
      <% @textdesc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
